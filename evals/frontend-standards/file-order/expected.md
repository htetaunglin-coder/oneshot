# Expected decisions — file order

| Case | Expected decision |
| --- | --- |
| F01 new-notification-list | `notification-list.tsx` reads, from the first line down: imports; `const MAX_ITEMS = 20`; the `NotificationListProps` type; `export function NotificationList` (the component the file is named after, exported at the declaration); `function NotificationRow`; `function formatRelativeTime`. `MAX_ITEMS` sits above the main export because the main export reads it. The row and helper are `function` declarations, so the list can call the row and the row can call the helper while each sits below its caller. The page imports `NotificationList`. Fails if: `formatRelativeTime` or `NotificationRow` sit above `NotificationList`; the file ends with `export { NotificationList }`; `MAX_ITEMS` is defined below the main component; or the sub-component/helper are `const` arrows placed above the main component so they "exist before use". |
| F02 single-reader-label-map | A `const PAYMENT_METHOD_LABEL: Record<PaymentMethod, string> = { card: "Card", bank_transfer: "Bank transfer", paypal: "PayPal", invoice: "Pay by invoice" }` is added (importing `PaymentMethod`), and `InvoicePayment` renders `PAYMENT_METHOD_LABEL[invoice.paymentMethod]`. Because only `InvoicePayment` reads it, the map sits directly above `function InvoicePayment`, after `InvoiceTotals`, not in the block under the imports. `InvoiceSummary`, `InvoiceTotals`, and their positions are unchanged. Fails if: the map is placed under the imports alongside `InvoiceSummaryProps` even though one sub-component reads it; the map is placed inside `InvoicePayment`'s body as a per-render object; or the change reorders existing declarations. |
| F03 second-reader-status-label | The `<button>` gets `aria-label={`${task.title}, ${STATUS_LABEL[task.status]}`}` and the badge gets `aria-hidden`. Because `TaskCard` now reads `STATUS_LABEL` too, the constant moves in the same change from directly above `StatusBadge` to the section under the imports (beside or above `TaskCardProps`, before `export function TaskCard`). `StatusBadge` stays below `TaskCard`. Fails if: `STATUS_LABEL` is left between `TaskCard` and `StatusBadge` after gaining a second reader; the label lookup is duplicated (a second map or an inline ternary in `TaskCard`) to avoid moving it; or `StatusBadge` is hoisted above `TaskCard`. |
| F04 add-footer-subcomponent | `ReportPanelProps` gains `onExport: () => void`; `ReportPanel` renders `<Header/>`, `<Body/>`, then `<Footer generatedAt={report.generatedAt} onExport={onExport} />`. A `type FooterProps = { generatedAt: Date; onExport: () => void }` sits directly above `function Footer`, and `Footer` is a `function` declaration placed after `Body` at the end of the file, matching the order the panel renders them. Fails if: `Footer` is inserted above `Header` or between `Header` and `Body`; `FooterProps` is placed at the top of the file next to `ReportPanelProps` even though only `Footer` uses it; or `Footer` is written as a `const` arrow above `ReportPanel`. |
| F05 cva-reads-sizes | `import { cva } from "class-variance-authority"` is added; `const badgeVariants = cva("inline-flex items-center rounded-full border font-medium", { variants: { size: SIZES (or `{ sm: SIZES.sm, md: SIZES.md }`), tone: { neutral: "border-border", danger: "border-red-300 bg-red-50 text-red-700", warning: "border-amber-300 bg-amber-50 text-amber-800" } } })`. `badgeVariants` sits in the section under the imports, below `SIZES` (it runs at import time and reads `SIZES`, so it must come after it) and above `export function StockLevel`. Both badges call `badgeVariants({ size, tone })` with `tone` chosen from `quantity === 0` / reorder state. Fails if: `badgeVariants` is placed above `SIZES` (a runtime `ReferenceError` from the TDZ) or below the main component/sub-components; `SIZES` is inlined into the `cva` and the constant deleted while the task said to reuse it; or a second copy of the size classes is written. |
| F06 arrow-file-add-helper | `ShiftList` calls `groupByDay(shifts)` and renders, per group, an `<h3>` with `group.day` followed by the `<ul>` of `ShiftRow`s. `groupByDay` is a `function` declaration (`function groupByDay(shifts: Shift[]): Array<{ day: string; shifts: Shift[] }>`), placed after `ShiftRow` at the end of the file; hoisting lets `ShiftList` call it from above. The day label is built from `startsAt` (e.g. `toLocaleDateString(undefined, { weekday: "short", day: "numeric", month: "short" })`) and insertion order is preserved (e.g. a `Map`). The existing `const ShiftList`/`const ShiftRow` arrows may stay as they are or be converted to `function` declarations; either is fine. Fails if: `groupByDay` is a `const groupByDay = (...) =>` placed above `ShiftList` because "a const must come first"; the helper is placed between `ShiftList` and `ShiftRow`; or the helper is defined inside the component body on every render. |
| F07 page-metadata-subcomponent | `import type { Metadata } from "next"` is added and `export const metadata: Metadata = { title: "Project · Acme" }` sits in the section under the imports (beside `PageProps`, before `export default async function ProjectPage`). `ProjectPage` renders `<ProjectMeta ownerName={project.ownerName} createdAt={project.createdAt} />` (or `project={project}`) under the `<h1>`. `ProjectMeta` is a non-exported `function` declaration placed below `ProjectPage`. `export default` stays on `ProjectPage` only. Fails if: `metadata` is placed at the bottom of the file or below the page component; `ProjectMeta` is placed above `ProjectPage`; `ProjectMeta` is exported or written as a `const` arrow above the page; or the default export is moved to an `export default ProjectPage` line at the bottom. |
| F08 export-list-card-footer | `CardFooter` is declared as `export function CardFooter({ className, ...props }: React.ComponentProps<"div">)` with `data-slot="card-footer"` and `cn("flex items-center gap-2 px-6 pb-6", className)`, placed after `CardContent`. Because the change touches the file's export surface, the trailing `export { Card, CardHeader, CardContent };` is removed and the three existing functions become `export function Card`, `export function CardHeader`, `export function CardContent` in the same change. The billing page imports `CardFooter` and replaces the raw `div`. Fails if: `CardFooter` is a plain function appended to the bottom export list (`export { Card, CardHeader, CardContent, CardFooter }`); `CardFooter` is inserted above `Card` or between `Card` and `CardHeader`; or the export list is kept while `CardFooter` alone is exported inline, leaving two export styles in one file. |


## Corrections after run 1 (2026-09-14)

F08: the key was wrong. The file is in `src/components/ui`, which the skill treats as registry-owned (colocation exempts it; the shadcn registry emits bottom export lists). Corrected expectation: the new component is declared as a plain `function` in nesting order and is added to the existing bottom export list; the list stays. Converting the file to inline exports is a fail unless the reply names the registry exception and the user asked for it. A run that converted the list under the old key is marked ⚠️ key defect, not a rule failure.
