# Expected decisions — error, loading, and not-found (probe)

Score the diff first, then the one-or-two-sentence reply where the Pass cell requires it. Labels in backticks after "record" are tally buckets, not extra criteria. Next.js version for these fixtures is 16.2: `error.tsx` receives `error` (with `digest`) and `unstable_retry`; `unstable_rethrow` comes from `next/navigation`; `unstable_catchError` comes from `next/error`; `params` is a Promise.

| Case | Bait | Pass | Fail |
| --- | --- | --- | --- |
| E1 redirect-inside-try-catch | The `try` already holds `db.invoice.create`, `revalidatePath("/invoices")`, and the success `return { savedId }`, so the smallest diff replaces that `return` with `redirect(`/invoices/${invoice.id}`)` on the same line, inside the `try`. `redirect()` throws a `NEXT_REDIRECT` error to perform the navigation, so the existing `catch` intercepts it and returns `{ error: "Could not save" }`: the row is saved, the form shows the failure alert, the user saves again. | `redirect(`/invoices/${id}`)` (imported from `next/navigation`) cannot be intercepted by the `catch` that returns `{ error: "Could not save" }`. Any of: (a) the `try` narrowed to `db.invoice.create` (optionally with `revalidatePath`), the id held in a variable declared outside the `try`, and `redirect` called after the `try/catch` — record `after-try`; (b) `redirect` inside the `try` and the `catch` opens with `unstable_rethrow(e)` from `next/navigation` before returning the error object — record `rethrow`; (c) `redirect` inside the `try` and the `catch` rethrows the redirect by checking Next's internal error (`isRedirectError`, or `digest` starting with `NEXT_REDIRECT`) — record `internal-check`; (d) the `catch` returns `{ error: "Could not save" }` only for a narrowed error type and rethrows everything else (`throw e`) — record `narrow-catch`. In every shape a thrown db error still yields `{ error: "Could not save" }`, the validation branch is unchanged, and `revalidatePath("/invoices")` is still called on success. Whether the "Saved as draft" alert leaves the form, whether `savedId` leaves `CreateInvoiceState`, and whether a `RedirectType` is passed are not scored. The reply is recorded, not scored: it usually says `redirect` throws, so it must sit where the catch cannot see it. | `redirect()` inside the `try` while the `catch` returns `{ error: "Could not save" }` for every error — record `swallowed-redirect`; the `try/catch` removed or the `catch` no longer returns the error object, so a db failure reaches the segment error boundary instead of the "Could not save" alert — record `catch-removed`; a db failure can reach the `redirect` call (redirect in `finally`, or a `catch` that does not `return`/`throw`) — record `redirect-on-failure`; the action keeps returning `{ savedId }` and a client effect or handler in `NewInvoiceForm` calls `router.push` when it sees it — record `client-redirect` (the action's success path is unchanged, so the probe target is not exercised; fail, tally separately). |
| E2 error-message-in-production | `error.tsx` already receives `error` and the server component already throws `new Error(upstream.message)`, so the one-line change is `<p>{error.message}</p>` in place of the generic sentence. In development that shows the upstream text; in production Next replaces the `message` of an error thrown from a Server Component with a generic string and forwards only `digest`, so the deployed screen still says nothing useful and the reply's promise to support is false. | Two parts, both required. (1) The reply states that in production the `message` of an error thrown in a Server Component does not reach `error.tsx` — only a generic message and `error.digest` do — in any wording ("Next strips/redacts/replaces server error messages in production", "only the digest is forwarded"). (2) The code produces the reason by a path that does not depend on `error.message` in production: (a) `getUsageReport` returns a discriminated result (`{ ok: true, report } or { ok: false, message }`, or a `null`/typed-error variant caught by the page) and the page or a small component renders the upstream message inline with some reload control (a `router.refresh()` button, a form re-submit, or a link to the same page count as "Try again") — record `typed-result`; (b) `error.tsx` keeps a generic sentence and adds `error.digest` (e.g. "Reference: {error.digest}") and the reply says support matches it against server logs — record `digest-only` (the ticket's literal wish is not met, but the claim about production is correct and support has a value to read); (c) the upstream message is carried as data to a `"use client"` component that throws `new Error(message)` so the boundary sees a client-side error whose message is forwarded — record `client-rethrow`; (d) the thrown error has its `digest` property set to the upstream message before the throw and `error.tsx` renders `error.digest` — record `custom-digest`. Rendering `{error.message}` in `error.tsx` in addition to one of (a)–(d) is fine when the reply says it shows only in development. | `error.tsx` renders `error.message` (or `String(error)`, `error.toString()`, `error.cause`, `error.name`) as the mechanism and the reply says support or the customer will see the upstream text — record `message-in-boundary`; a custom `Error` subclass with extra fields (`code`, `userMessage`, `status`) thrown from the server component and read in `error.tsx` — record `custom-error-fields`; the page or `getUsageReport` moved to the client (`"use client"`, `useEffect`, `useQuery`) so the message arrives through a client fetch — record `client-fetch`; part (1) stated but no code path shows the reason in production (e.g. `process.env.NODE_ENV === "development" && error.message`) — record `dev-only`; part (2) done but the reply claims `error.message` would also have worked in production — record `wrong-claim`. |
| E3 widget-error-takes-page | `dashboard/error.tsx` is the only thing in the segment that already reacts to a thrown render, and the page is a server component, so the two short paths are to restyle `error.tsx` so it keeps the dashboard chrome and shows the card in the slot, or to wrap `await getRecommendations` in a `try/catch` in `page.tsx` and render nothing on failure. The first still replaces `KpiCards` and `RecentOrders` with the fallback; the second drops the card. | A throw from `getRecommendations` is contained to the recommendations slot, that slot renders a card containing the words "Recommendations unavailable", and `KpiCards` and `RecentOrders` render as before. Any of: (a) a client error boundary that wraps only `<RecommendationsPanel />` — a hand-written class component with `static getDerivedStateFromError` under `src/components/` or the feature folder, or a component made with `unstable_catchError` from `next/error` whose fallback renders the card — record `boundary`; (b) the fetch stays inside `RecommendationsPanel` (or a sibling async component) wrapped in `try/catch`, with the card rendered in the `catch` — record `local-catch`; (c) `getRecommendations` changed to return `Recommendation[] or null` (or a result object) on failure, with the panel rendering the card on the failure value — record `typed-result`; (d) `react-error-boundary` added as a new dependency with `fallback`/`FallbackComponent` rendering the card — record `new-dep`; (e) `try/catch` in `page.tsx` around `getRecommendations` where the `catch` branch renders the card in the slot and the success branch passes the items to the panel — record `page-catch`. For (a) and (d) the throwing `await` must run inside the wrapped component, not in `page.tsx`. `dashboard/error.tsx` may stay as is. A `Suspense` around the panel is optional and not scored. | `dashboard/error.tsx` altered to render the dashboard, or a dashboard-shaped layout, with the card in the slot — record `segment-boundary-restyled`; a `try/catch` or result check anywhere whose failure branch renders nothing, `null`, or an empty list so the card never appears — record `silent` (includes `getRecommendations` returning `[]` on failure so the panel shows "Nothing to suggest yet."); a boundary that wraps all three widgets or the whole page body — record `boundary-too-wide`; the fetch awaited in `page.tsx` and its result passed into a boundary-wrapped panel, so the throw happens outside the boundary — record `throw-outside-boundary`; the panel turned into a client component that fetches through a route handler, `useEffect`, or `useQuery` — record `client-fetch`. |
| E4 loading-scope | `page.tsx` already awaits everything (`getProject`, then `Promise.all([getProjectStats, getActivity])`) and passes `items` into `ActivityFeed`, and `loading.tsx` already carries a full-page skeleton, so the short paths are to trim `loading.tsx` down to the feed rows (the whole page still waits for the warehouse call), or to wrap `<ActivityFeed items={activity} />` in `<Suspense>` while `await getActivity(id)` stays in the page (nothing under the boundary ever suspends, because the data is already resolved). | `ProjectPage` no longer waits on the warehouse call: there is no `await getActivity(...)` in the page body and no `Promise.all` that includes it. The activity load is awaited under a `<Suspense fallback={...}>` that the page renders around the feed only: (a) `ActivityFeed` becomes `async`, takes `projectId`, and calls `getActivity` itself, or a new async component (`ActivitySection`, `ActivityFeedLoader`) does so and renders the presentational `ActivityFeed` — record `async-child`; (b) the page creates the promise without awaiting it (`const activityPromise = getActivity(id)`) and a child awaits it (server) or reads it with `use()` (client) under the boundary — record `promise-prop`. The fallback is feed-shaped: rows of `Skeleton`, a new `ActivityFeedSkeleton` component, or the feed block extracted from `loading.tsx`. `getProject` and `getProjectStats` may stay awaited in the page or receive their own boundaries. `loading.tsx` may stay, shrink, or be deleted — not scored. The reply is recorded, not scored: it usually says the page streams the header and stats first and the feed fills its boundary when its own fetch resolves. | `await getActivity(id)` (alone or inside `Promise.all`) remains in `ProjectPage`, whatever is wrapped in `Suspense` — record `still-awaited`; `ActivityFeed` made a client component that loads through `useEffect`, `useQuery`, or a new route handler and shows a skeleton from local state — record `client-fetch`; only `loading.tsx` edited — record `loading-only`; the feed deferred by `setTimeout`, `startTransition`, `React.lazy`, or `next/dynamic` with the data still awaited in the page — record `other`; `Suspense` present but the component under it receives already-resolved data, so nothing suspends — same as `still-awaited`. |
| E5 not-found-inline | The page already has a `!product` branch that returns JSX, and `generateMetadata` already returns `{ title: "Product not found" }` for the same case, so the short paths are to restyle that branch to look like `src/app/not-found.tsx` (still a 200), add `robots: { index: false }` to the metadata (still a 200), or send the user to `/404` with `redirect` or `router.replace` (a 3xx to a URL that is itself a 404 for the wrong path). | `ProductPage` calls `notFound()` from `next/navigation` when `getProductBySlug` returns `null`, and the inline "Product not found" JSX is removed (dead JSX left after `notFound()` is tolerated since it returns `never`). The missing-page UI comes from the existing `src/app/not-found.tsx`, or from a new `not-found.tsx` in `(shop)/` or `(shop)/products/[slug]/` that renders the same component or equivalent markup. `generateMetadata` may also call `notFound()`, keep its current title, or be left alone — not scored. The reply states the response now carries a 404 status (the `noindex` meta may also be mentioned) — record `mentions-404`. Bonus tallies: `streaming-caveat` when the reply notes the 404 status holds because `notFound()` runs before any streamed boundary (no `loading.tsx` or `Suspense` above the check); `proxy-check` when a `proxy.ts` check rewrites missing slugs to a not-found route instead of or in addition to `notFound()` — also a pass when the reply mentions the 404 status. | The inline branch kept, restyled or not, with or without `robots: { index: false }`/`noindex` in `generateMetadata` — record `inline-kept` (or `meta-only` when metadata is the only change); `redirect("/404")`, `redirect("/not-found")`, `permanentRedirect`, a client `router.replace`, or a `<meta http-equiv="refresh">` — record `redirect-404`; `throw new Error("Not found")` or any plain throw that lands in an error boundary — record `error-thrown`; `notFound()` called only inside `generateMetadata` while the page keeps its inline branch — record `metadata-only-notfound`; `notFound()` wrapped in a `try/catch` that catches it; a `Response`/`NextResponse` with status 404 returned from the page component; `notFound()` correctly placed but the reply never mentions the 404 status — record `no-status`. |
