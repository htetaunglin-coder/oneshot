# Expected decisions — conditional rendering (holdout)

| Case | Bait | Expected decision |
| --- | --- | --- |
| S1 dashboard-body-states (iife-by-precedent) | The body already renders through an inline function call with three `if`/`return` lines, the comment says "add new states to this block", and the request says "follow the note", so the smallest diff is a fourth `if (data.plan === "free") return <UpgradePrompt plan={data.plan} />;` inside that block. | The inline function call is removed from the JSX. The four body states (loading, error, free plan, chart) are decided before the `return`, most simply as a variable (`let body: ReactNode; if (isLoading) body = <Skeleton .../>; else if (error) body = ...; else if (data.plan === "free") body = <UpgradePrompt .../>; else body = <UsageChart .../>;`) and placed once as `{body}` inside `CardContent`; a module-level `function UsagePanelBody({ ... })` with early returns that receives `isLoading`, `error`, `data`, `period`, `refetch` also passes. The card header, period selector, and footer render in every state, so no whole-component early return replaces the body switch. The comment is updated or dropped to match the new shape. The reply says why the inline call went: a function invoked inside JSX is the last resort for one branch, and four keyed states belong in a variable or a sub-component decided above the JSX. Fails if: the immediately invoked function stays, or a fourth `if` is added inside it, or the states become a chain of `? :` inside the JSX, or the header/footer stop rendering in any state, or the reply justifies keeping the call by the file comment. |
| S2 long-enterprise-block (extract-for-length) | The enterprise block is 35 lines, the request adds two more rows, and the reviewer said "getting long, hard to scan", so the reflex is a new `EnterpriseContractSection` (or `EnterprisePanel`) component, in this file or a sibling file, rendered as `{plan === "enterprise" ? <EnterpriseContractSection contract={contract} /> : null}`. | The two rows (`SSO` with `contract.sso.provider` plus `" (enforced)"` when `contract.sso.enforced`; `Support channel` as `<a href={contract.supportChannel.href}>` with the label) are added to the existing `<dl>`. The block stays in this component: either inline under `{plan === "enterprise" ? (...) : null}` (or `{plan === "enterprise" && (...)}`, since the left side is a comparison), or hoisted into a variable before the JSX (`const contractSection = plan === "enterprise" ? (<section>...</section>) : null;`) and placed as `{contractSection}`. The `(enforced)` suffix may be a small template or a `? :` with `null`; both pass. No new component is created. The reply answers the reviewer by saying the block is long but flat (no state, no hooks, no nested condition), so length alone does not earn a component and a variable (or leaving it inline) is the right size. Fails if: a new component is introduced for the enterprise block (in this file or a new file), or the block is moved out of the feature, or a `? :` is nested inside the `enforced` suffix in a way that nests inside the outer plan check, or the reviewer comment is answered by silence with the block simply grown. |
| S3 third-role-badge (nested-ternary-is-fine-when-short) | The badge is one line, the change is one more role, and the request says "keep it small", so the instinct is a third level: `{author ? (author.isAdmin ? <AdminBadge /> : author.isModerator ? <ModeratorBadge /> : <UserBadge />) : <GuestBadge />}`. | The nesting is removed. Acceptable shapes: (a) a variable before the JSX with a flat `if`/`else if` chain that assigns `badge`, ending in `<GuestBadge />` for `null`; (b) a small role resolver (`function roleOf(author): "guest" \| "admin" \| "moderator" \| "user"`) plus a `Record<Role, ReactNode>` or `Record<Role, ComponentType>` map; (c) a module-level `function RoleBadge({ author })` with early returns (`if (!author) return <GuestBadge />; if (author.isAdmin) return <AdminBadge />; if (author.isModerator) return <ModeratorBadge />; return <UserBadge />;`) rendered as `<RoleBadge author={author} />`. Admin wins over moderator. `displayName` fallback is untouched. The reply says a third role made the expression a chain of more than two outcomes, so it moved to a flat chain, a map, or a sub-component rather than a second level of `? :`. Fails if: any `? :` sits inside another `? :`, or the third role is added by chaining a `? :` in the false branch of the inner one, or a `RoleBadge` is placed in `src/components/ui` (it is feature-specific), or `isModerator` is checked before `isAdmin`. |
| S4 return-null-from-child | `PromoBanner` already opens with `if (!user.eligibleForPromo) return null;`, so the two-line diff is `if (!user.eligibleForPromo \|\| user.dismissedPromo) return null;` (or a second guard line) and the parent is untouched. | Presence moves to the parent. `dashboard-header.tsx` computes a boolean (`const showPromo = user.eligibleForPromo && !user.dismissedPromo;`) and renders `{showPromo ? <PromoBanner user={user} /> : null}` (or `{showPromo && <PromoBanner user={user} />}`; both pass because the left side is a boolean). `promo-banner.tsx` loses its `return null` and renders unconditionally; its props may narrow to what it reads (`user`, or `{ userId, promoEndsAt }`). Behaviour: hidden when ineligible or dismissed, Dismiss still calls `dismissPromo(user.id)`. The reply says a component that is placed by a parent should not decide its own presence, and that the two conditions are one boolean owned by the parent. Soft pass: the banner keeps a single `return null` and the reply states the reason (it is a self-gating promo unit that is dropped into several pages and must own eligibility so no caller can forget the check); the parent stays untouched in that case. Fails if: a second condition is added to the child's `return null` (combined or as a second guard) with no stated reason for the child owning presence, or the condition is duplicated in both parent and child, or the parent hides the banner but the child still keeps its `return null` guard. |
| S5 effect-after-guard | The file already runs `useQuery` and `useState` below `if (!session) return <SignIn />`, the request says signed-out visitors must not send the event, and `session.user.id` is only narrowed after the guard, so the natural spot is a `useEffect(() => { track("account_viewed", { userId: session.user.id }); }, [session.user.id]);` placed after the early return next to the other hooks. | Every hook sits above the first `return`. Two passing shapes: (a) in this component, `useSession`, `useQuery`, `useState`, and the new `useEffect` all run before `if (!session) return <SignIn .../>`; the query uses `enabled: !!session` (or `session !== null`) and `session?.user.id` in its key, and the effect guards inside (`if (!session) return; track("account_viewed", { userId: session.user.id });`) with `[session?.user.id]` (or `[session]`) as deps; (b) `AccountOverview` keeps only `useSession` and the guard, and the signed-in part becomes a module-level `function SignedInAccount({ session }: { session: Session })` that owns `useQuery`, `useState`, and the `useEffect`, which then fires once per mount with no guard. The event does not re-fire on tab change (tab state is not in the effect deps). `AccountSkeleton` and the tabs are unchanged. The reply names the Rules of Hooks (hooks run in the same order every render, so none may follow a conditional return) and says the existing hooks were moved for the same reason. Fails if: any hook (`useQuery`, `useState`, `useEffect`) sits below a `return`, or the new effect is added after the guard while the existing hooks stay where they were, or the effect fires on every tab change, or the event fires for a signed-out visitor, or the reply says the existing placement is fine because the session is stable. |
