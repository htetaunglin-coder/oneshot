# Expected decisions — state location (probe)

Score the diff first, then the one-or-two-sentence reply where the Pass cell requires it. Labels in backticks after "record" are tally buckets, not extra criteria.

| Case | Bait | Pass | Fail |
| --- | --- | --- | --- |
| P1 module-scope-request-state | The session store is already a module-level `create()` singleton holding `orgId`/`orgName`, `SessionHydrator` already calls `useSessionStore.setState` from the layout, and the ticket says "add `plan` next to it and hydrate it the same way", so the smallest diff is a third field in `SessionState`, one more prop on `SessionHydrator`, and `useSessionStore((s) => s.plan)` in `ExportButton`. | `plan` is not added to `useSessionStore` or to any other module-scope variable. It reaches `ExportButton` per request by one of: (a) a client `SessionProvider` under `src/lib/session/` that creates its own store instance inside the component (`useState(() => createStore(...))`, `useRef`, or `useMemo` with `createStore` from `zustand`), exposes it by React context, and is read with `useStore(store, selector)` or a wrapper hook, the layout rendering the provider with `plan` (and usually `orgId`/`orgName`) from `getSession()`; (b) a plain React context provider valued from the layout's `session`, read with `useContext`/`use`; (c) `plan` read with `getSession()` in `src/app/(app)/reports/[id]/page.tsx` and passed as a prop through `ReportHeader` to `ExportButton`. Existing `orgId`/`orgName` may stay where they are or move into the same provider. On `free` the button renders disabled inside a `Tooltip` reading "Upgrade to export"; other plans unchanged. The reply usually gives the reason in these terms: a module-level store is one object per server process shared by every request that renders on the server, so a value seeded from one user's session can be read while rendering another user's page, which is why per-request values are created inside a component or passed down. The reason is recorded, not scored: the rule asks for the shape, not the sentence. | `plan` added to `SessionState` and written by `SessionHydrator` (in render, `useEffect`, or `useLayoutEffect`) — record `module-store`; a module-level `let` or object mutated at runtime holds `plan`; `plan` kept in the module store behind a `typeof window` guard or a client-only effect; the reply says `"use client"` keeps the module off the server; the tooltip/disabled behaviour for `free` is missing or wrong. |
| P2 query-data-copied-to-store | `OrdersTable` already copies the query result into `useOrdersStore` under a "keep the store in sync" comment and `OrderDetailPanel` already reads `s.orders` from the store, so the shortest change is `useOrdersStore((s) => s.orders.filter((o) => o.status === "open").length)` in `OrdersNavItem`. | `OrdersNavItem` (directly or through a small hook in `src/features/orders/hooks/`) gets the count from TanStack Query: `useOrders()` with the count derived from `data`, or `useQuery`/`useOrders` with a `select` over `ordersQueryKey` that returns the open count. It does not read `orders` from the store and introduces no effect or subscription that writes query data into the store. Badge omitted when the count is 0, while loading, and on error. Optional tallies: `named-copy` when the reply says the `useEffect` in `OrdersTable` duplicates the query cache; `removed-copy` when `orders`/`setOrders` are removed from the store and `OrderDetailPanel` reads from `useOrders()` (`selectedId`/`select` stay in the store). | `OrdersNavItem` reads `orders` or any count from `useOrdersStore` — record `store-read`; a new store field such as `openCount` written from the query by an effect or subscription — record `second-copy`; the `setOrders` effect moved or duplicated into a layout, sidebar, or provider so the store fills on every page; the count taken from `queryClient.getQueryData(ordersQueryKey)` during render with no subscription; the reply defends the store as the source for orders. |
| P3 derived-value-synced-by-effect | `subtotal` and `total` already live in `useState` and are recomputed in a `useEffect` on `[items]`, so the smallest diff is `const [promo, setPromo] = useState<Promo or null>(null)`, `promo` added to the dependency array, the discount computed inside the effect, `setTotal(next - discount + SHIPPING_CENTS)`, possibly a third `useState` for `discount`. | The applied code is held in one `useState<Promo or null>` (holding it in the cart store instead is not scored). `subtotal`, `discount` (`Math.round(subtotal * promo.percentOff / 100)`, `0` with no promo), and `total` are computed in the render body from `items` and `promo`, as plain `const`s or inside `useMemo`, and the `useState`/`useEffect` pair for `subtotal`/`total` is deleted, so no `useEffect` in the file calls a state setter. `PromoCodeInput` mounted above the `<dl>`; a `Discount (CODE)` row appears only while a promo is applied and shows a negative amount; shipping unchanged. Minimal pass, record `kept-old-sync`: the pre-existing `[items]` effect is left untouched, but `promo`, `discount`, and the discounted total are computed in render and no effect or state setter is added for them. | `promo` or `discount` enters the existing effect's dependency array or body — record `effect-extended`; a new `useState` for `discount` or `total` written from any `useEffect`/`useLayoutEffect` — record `second-synced-state`; a new hook such as `useCartTotals` that computes totals with effect-plus-state; the discount is not `percentOff` of the subtotal or the total does not drop by it. |
| P4 prop-mirrored-into-state | `ProfileCard` holds `const [name, setName] = useState(user.name)` under a `TODO(PROF-212)` comment, so the shortest fix that "keeps the plan" is `useEffect(() => { setName(user.name); }, [user.name]);`. | `ProfileCard` renders `user.name` directly in both places (`<p>` text and `initials(user.name)`), the `name` state and the `useState` import are removed, and no `useEffect` is added anywhere for this bug. `AccountView` and `SettingsForm` may stay unchanged; adding `key={user.id}` to `<ProfileCard>` while the state is gone is harmless. The reply gives the reason: `useState(user.name)` reads the prop only as the initial value on first mount, later `user` props never reach it, so display text should read the prop on every render. A code-correct change whose reply gives no initial-value reason scores fail; record `no-reason`. | `useEffect`/`useLayoutEffect` calling `setName(user.name)` on `[user.name]` or `[user]` — record `effect-sync`; state kept and the card remounted with `key={user.name}` or `key={user.id}` in `AccountView` — record `key-workaround` (`user.id` does not change on rename, so that one also leaves the bug); state kept and synced during render against a stored previous prop — record `render-sync`; change made only in `AccountView`/`SettingsForm` (`setQueryData`, `refetch`, `router.refresh()`) while `ProfileCard` still holds `name` in state. |
| P5 filter-state-location | `category` and `sort` are already two `useState` calls in `ProductList`, so the smallest change is to leave them and wrap them in `localStorage` (or Zustand `persist`) reads and writes, which fixes the refresh half of the ticket and silently misses the shared-link half. Record which default the solver picked. | The selection is carried in the page URL. On load `category` and `sort` are read from the query string: `useSearchParams()` in `ProductList`, or the page's `searchParams` prop (a Promise in this Next version) awaited and passed to `ProductList` as initial values, or a URL-state library such as `nuqs`; defaults `all`/`newest` apply when a param is absent or not one of the allowed values. Every change writes the query string: `router.replace`/`router.push` from `next/navigation` with `pathname + "?" + params`, `<Link href={{ pathname, query }}>`, or `window.history.replaceState`/`pushState`. Param names, replace versus push, whether defaults are stripped from the URL, and whether `useSearchParams` is wrapped in `<Suspense>` are not scored. A local `useState` initialised from the URL and mirrored to the URL on each change passes as long as a pasted URL restores both values. Record `url-hook`, `url-page-prop`, or `url-library`. | Any solution in which a copied URL does not carry the selection: `localStorage` or `sessionStorage` — record `local-storage`; cookies via `document.cookie` or server `cookies()` — record `cookie`; Zustand `persist` or any store — record `store`; `useSyncExternalStore` over storage; a hybrid where only one of `category`/`sort` is in the URL; the URL is written on change but not read on load, so the list still starts at defaults after refresh. |
