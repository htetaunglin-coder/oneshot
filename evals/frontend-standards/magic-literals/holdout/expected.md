# Expected decisions — magic literals (holdout)

| Case | Bait | Expected decision |
| --- | --- | --- |
| N1 name-everything | "Clean up the numbers" reads as "no bare numbers", so the reflex names every one: `CENTS_PER_DOLLAR = 100`, `PERCENT_MULTIPLIER = 100`, `DECIMAL_PLACES = 2`, maybe `ZERO_SHIPPING = 0`. | Exactly two values get module-level `CONSTANT_CASE` names at the top of `pricing.ts`: the tax rate (e.g. `TX_SALES_TAX_RATE = 0.0825`) and the free-shipping threshold with its unit in the name (e.g. `FREE_SHIPPING_THRESHOLD_CENTS = 50_00`). `cents / 100`, `* 100` in the percent formula, `toFixed(2)`, `Math.round`, `<= 0`, `? 0 :` all stay inline because the line already says what they are. Names carry meaning and unit, not the value (`EIGHT_POINT_TWO_FIVE_PERCENT` is a fail). Behaviour unchanged. The reply says the rate and the threshold are business values a reader cannot decode from the line, and the rest are formula constants. Fails if: `100` or `2` receive a constant (any name), or the tax rate or the threshold stays inline, or a constant is named after its value, or the threshold name lacks a unit, or the constants are moved to a shared file outside the feature. |
| N2 constants-bag | `src/lib/constants.ts` already exists, three files import from it, and two members files need the same number, so the natural move is `export const MEMBERS_PAGE_SIZE = 25` appended to that file. | The page size is a `CONSTANT_CASE` value owned by the members feature. Acceptable homes: exported from the top section of `use-members.ts` and imported by `members-table.tsx`; or a feature-local module such as `src/features/members/lib/pagination.ts` (any name) read by both. The hook accepts `page`, passes `{ orgId, page, pageSize: MEMBERS_PAGE_SIZE }` to `fetchMembers`, and includes `page` in the query key. The table computes the shown range and the last page from `data.total` and the same constant, never from a second literal `25`. `src/lib/constants.ts` is untouched. The reply names feature ownership as the reason the value did not join the shared file. Fails if: `MEMBERS_PAGE_SIZE` (any name) lands in `src/lib/constants.ts`, or `25` is written twice (hook and table), or the range text computes from a literal, or the hook hard-codes the size while the table imports a constant. |
| N3 enum-instinct | "A proper type ... so the compiler catches typos", with the same three strings compared in three files and rendered in a picker, calls up `enum NotificationKind { Info = "info", Warning = "warning", Error = "error" }`. | In `types.ts` (or a sibling module in `lib`): `export const NOTIFICATION_KINDS = ["info", "warning", "error"] as const;` and `export type NotificationKind = (typeof NOTIFICATION_KINDS)[number];`. `Notification.kind` becomes `NotificationKind`; `useNotifications(kind?: NotificationKind)`; the filter's `value` and `onChange` use `NotificationKind | undefined` and the picker renders `NOTIFICATION_KINDS.map(...)` for the three items (a label map keyed by kind is fine; the `"all"` sentinel may stay a literal in the picker). Comparisons in `notification-item.tsx` may stay `=== "error"` against the union or become a `Record<NotificationKind, ...>` lookup; both pass. No `enum` anywhere. The reply says the array is the single source, the type is derived from it, and the picker iterates it. Fails if: an `enum` is introduced (string or numeric), or the union is written by hand next to a separate array so the two can drift, or the picker still spells out the three `<SelectItem>` strings by hand, or the type is exported but `kind` stays `string` in any of the three files. |
| N4 rename-the-value | The request says "raise the limit to 50" and the file has `THIRTY = 30`. Smallest diffs: change `THIRTY = 50` (breaks the timer), or add `const FIFTY = 50` and use it for `take` while `THIRTY` stays for the timer. | `THIRTY` is deleted. Two constants replace it, each named by meaning and unit: a list limit (e.g. `FEED_LIMIT = 50` or `ACTIVITY_TAKE = 50`) used at `take:`, and a poll interval in milliseconds (e.g. `POLL_INTERVAL_MS = 30 * 1000` or `30_000`) used at `setTimeout`. Both sit in the top section as module-level `CONSTANT_CASE`. The poll interval stays at 30 seconds. The reply says the one constant served two unrelated meanings and was split before the limit changed. Fails if: any value-named constant survives (`THIRTY`, `FIFTY`, `THIRTY_SECONDS`), or the poll interval changes from 30 seconds, or `50` is written inline at `take:` while `THIRTY * 1000` remains, or the interval constant lacks a unit in its name. |
| N5 literal-once-is-fine | The sibling hook opens with `IDLE_LIMIT_MS` at the top, so consistency pressure writes `const ONE_SECOND_MS = 1000;` (and often `const ZERO = 0;` or `const MIN_REMAINING = 0;`) above `useCountdown`. | `setInterval(tick, 1000)` (or `setTimeout` chained) with the `1000` inline; a local `const intervalMs = 1000` inside the hook is also fine. `0` stays inline in `Math.max(0, ...)`, `remaining <= 0`, and the stop check. No module-level constant is added for either value; the file has nothing between the imports and `export function useCountdown`. The hook keeps `remaining` in state, ticks once per second, clamps at zero, stops the interval at zero, `restart()` resets to `seconds` and restarts, and cleanup clears the interval on unmount. The reply says the one-second tick is a single-use duration whose unit is visible on the line and that zero needs no name. Fails if: `ONE_SECOND_MS`, `MS_PER_SECOND`, `ZERO`, `MIN_REMAINING`, or any equivalent module-level constant is created, or the tick is written as `1 * 1000` behind a name, or `45` from the caller is moved into the hook as a default. |

## Round 2

| Case | Bait | Expected decision |
| --- | --- | --- |
| N6 camelcase-precedent | The file opens with `const pageSize = 20; const maxTags = 5; const debounceMs = 300;` and the request says "keep it consistent with the rest of this file", so the new limit lands as `const maxAttachments = 10;` in the same block. | The attachment limit is a business limit read in two places (the disabled check and the counter text), so it gets one module-level name in `CONSTANT_CASE` (e.g. `MAX_ATTACHMENTS = 10`), and `10` is not written inline anywhere in the JSX. The three existing camelCase values may be renamed to `PAGE_SIZE`, `MAX_TAGS`, `DEBOUNCE_MS` because the change touches that block, or left as they are with the reply naming them as pre-existing inconsistency; both pass. The counter renders `{files.length}/{MAX_ATTACHMENTS} files` and the button disables at `files.length >= MAX_ATTACHMENTS`. The hidden `<input type="file" multiple>` and the `AttachmentList` wiring are otherwise ordinary. The reply says the existing camelCase names are not a precedent for a new module-level value. Fails if: the new limit is camelCase (`maxAttachments`, `attachmentLimit`), or `10` appears inline in the JSX or the disabled check, or the limit is a local `const` inside the component body (it does not change per render), or the reply justifies camelCase by file consistency. |
| N7 enum-by-request | The ticket says "add an `enum`" verbatim and gives the reason (typos like `"payed"` slipped through), so compliance is `enum InvoiceStatus { Draft = "draft", Sent = "sent", Paid = "paid", Void = "void" }` and the four files switch to `InvoiceStatus.Paid` and friends. | In `types.ts` (or a sibling module in `lib`): `export const INVOICE_STATUSES = ["draft", "sent", "paid", "void"] as const;` and `export type InvoiceStatus = (typeof INVOICE_STATUSES)[number];`. `Invoice.status` becomes `InvoiceStatus`; `useInvoices(status?: InvoiceStatus)`; the filter's props use `InvoiceStatus \| undefined` and the dropdown renders `INVOICE_STATUSES.map(...)` for the four items (a label map keyed by status is fine; the `"all"` sentinel may stay a literal in the filter, or take a local name). Comparisons in `invoice-row.tsx` and `use-invoices.ts` may stay `=== "paid"` / `!== "void"` against the union; the `"payed"` typo is now a compile error, which is what the ticket wanted. No `enum` anywhere. The reply says why it did not write an `enum` (an `enum` is not erasable syntax; the `as const` array plus derived union gives the ticket what it asked for: no raw `string`, one list, compile-time typos) and that the dropdown iterates the array. Fails if: an `enum` is written (string or numeric), or the union is written by hand next to a separate array so the two can drift, or the dropdown still spells out four `<SelectItem>` strings by hand, or `Invoice.status` stays `string`, or the reply declines the ticket without saying why `as const` satisfies it. |
| N8 loose-test-numbers | "A lot of loose numbers ... tidy them up" reads as "name every number in the file", so the reflex is `const SAMPLE_WEIGHT_KG = 2.5; const LONG_DISTANCE_KM = 120; const SHORT_DISTANCE_KM = 40; const TAX_RATE = 0.0825;` at the top of the test, with each `it` reading from the bag. | The fixture inputs (`weightKg: 2.5`, `distanceKm: 40`, `120`, `100`, `1`, `4`, `10`) and the expected outputs (`875`, `1675`, `650`) stay inline, because the test name on the same block carries their meaning ("above 100 km", "exactly 100 km", "short trip"). The one decoded value that repeats, `0.0825`, stops being three literals: the test imports `TX_SALES_TAX_RATE` from `./calculate-shipping` (preferred) or names it once at the top of the test file in `CONSTANT_CASE`, and the three `Math.round(n * ...)` assertions read it. `calculate-shipping.ts` is unchanged. The reply says fixtures are readable through the test names and only the repeated rate needed a name. Fails if: any fixture input or expected output becomes a module-level or `describe`-level constant (`SAMPLE_WEIGHT_KG`, `LONG_DISTANCE_KM`, `EXPECTED_SUBTOTAL`), or `0.0825` remains as more than one literal, or the implementation file is edited, or the reply treats the test file as exempt and changes nothing. |
| N9 card-image-width | The request names two components and a number, so the smallest diff is `width={720} height={405}` in `listing-card.tsx` and again in `featured-listing-card.tsx`, with the skeleton's `max-w-[640px]` and the validator's `640` either untouched or all three bumped together. | One shared design value, `CARD_IMAGE_WIDTH_PX = 720` (any name that says card, image, width, and the unit), lives once in the listings feature (a `lib` module such as `src/features/listings/lib/card-image.ts`, or exported from the top of one card and imported by the other) and both `<Image>` sites read it; the height is either a sibling `CARD_IMAGE_HEIGHT_PX = 405` or derived (`CARD_IMAGE_WIDTH_PX * 9 / 16`), both pass. The skeleton follows the same value instead of a second `720`: `style={{ maxWidth: CARD_IMAGE_WIDTH_PX }}`, a CSS variable set from the constant, or a Tailwind theme token (`@theme { --width-card-image: 720px }` with `max-w-card-image`); as a floor, `max-w-[720px]` with the reply noting it mirrors the constant passes. `validateDescription`'s `640` is untouched and is not given the image-width name. The reply says the width is a design decision reused across three sites and that the description length is an unrelated value that happened to match. Fails if: `validateDescription`'s `640` changes or is renamed to the image constant, or `720` is written as separate literals at the two `<Image>` sites, or the skeleton is left at `640` with no note, or the width constant goes to `src/lib`. |
| N10 second-role-check | `member-actions.tsx` already reads `viewer.role === "owner"`, and the new nav needs the same test, so the smallest diff is `viewer.role === "owner" \|\| viewer.role === "admin"` written inline in `settings-nav.tsx` and again in `member-actions.tsx`. | The privileged set gets one name in the workspace feature's `lib`: `export const PRIVILEGED_ROLES = ["owner", "admin"] as const satisfies readonly WorkspaceRole[];` (any equivalent, including a `Set`) and/or a predicate `export function canManageWorkspace(member: WorkspaceMember): boolean` built on it. Both sites use the shared thing: `member-actions.tsx` replaces `viewer.role === "owner"` (keeping `viewer.id !== member.id`), and `settings-nav.tsx` adds `{ href: \`${base}/billing\`, label: "Billing" }` only when the predicate holds (a filtered tab list or a conditional push, either is fine). No `enum`. The reply says a comparison against a string that now appears in two files became a named set or function so the two sites cannot drift. Fails if: two inline role comparisons remain in two files, or the set is duplicated (an array in one file and `\|\|` in the other), or the check is placed in `src/lib` or `src/components/ui`, or `"owner"` is compared inline in one file while the other imports the helper. |

## Round 3

| Case | Bait | Expected decision |
| --- | --- | --- |
| N11 platform-keys | The owner's "these string comparisons are getting scattered, please tidy" plus four `event.key === "..."` branches read as a request for a names block, so the reflex is `const KEYS = { ARROW_DOWN: "ArrowDown", ARROW_UP: "ArrowUp", ENTER: "Enter", ESCAPE: "Escape", HOME: "Home", END: "End" } as const` (or `ENTER_KEY = "Enter"` and friends), and while there `query === ""` becomes `EMPTY_QUERY` and `"checkbox"` gets a name too. | `Home` and `End` are added as two more branches that compare `event.key` against the literal `"Home"` / `"End"` (setting `activeIndex` to `0` and `visibleItems.length - 1`, with `preventDefault`). The tidy is structural, not nominal: a `switch (event.key)` with `case "ArrowDown":` ... `case "End":`, or a handler map keyed by the literal key names, both pass; an early-return chain also passes. `target.type === "checkbox"` and `query === ""` are unchanged. No module-level constant, object, or `enum` holds any key name, `"checkbox"`, or `""`. The reply says the key names and the input type are values the platform defines (`KeyboardEvent.key`, `HTMLInputElement.type`) so the meaning is already on the line, and that the review comment was answered by restructuring the branches. Fails if: any of `"ArrowDown"`, `"ArrowUp"`, `"Enter"`, `"Escape"`, `"Home"`, `"End"`, `"checkbox"`, or `""` gets a named constant, a keys object, or an `enum`, or the review comment is answered with a constants block, or the reply says the comment cannot be addressed without naming the strings. |
| N12 inbox-page-size | The request names one number, so the smallest diff is `pageSize = 40` in the hook and `Math.min(pageSize, 40)` in the action, two literals that must move together next time; or only the hook edit, leaving the clamp at 25 so every request still returns 25 rows. `delay = 300` sits in the same feature and may get swept into a `DEBOUNCE_MS` "while here". | One `export const INBOX_PAGE_SIZE = 40;` (any name that says inbox, page, size) lives once in the inbox feature, in `src/features/inbox/lib/` (its own module or the top of `list-messages.ts`), and both readers import it: the hook default becomes `pageSize = INBOX_PAGE_SIZE` and the clamp becomes `Math.min(pageSize, INBOX_PAGE_SIZE)`. Neither `25` nor `40` is written inline in either file. `useDebouncedValue(value, delay = 300)` is untouched; `search = ""` and `search === ""` are untouched. The reply says the page size is one limit enforced in two files (a default the server also clamps) so it needed one shared name, and that the debounce default is a parameter whose name already carries its meaning and is read nowhere else. Fails if: `40` appears as two literals, or the hook is changed while the clamp stays at `25`, or `delay = 300` becomes a constant, or the constant goes to `src/lib`, or the `"use server"` module imports the value from the `"use client"` hook file. |
| N13 processor-fee | The request names one number, "30 to 35", so the smallest diff is `+ 35` in place; `0.029` looks like part of a formula, next to `cents / 100` and `waitMs * 2`, so either the whole line stays bare or the tidy reflex names everything (`CENTS_PER_DOLLAR = 100`, `BACKOFF_FACTOR = 2`, `INITIAL_WAIT_MS = 500`). | Two module-level `CONSTANT_CASE` values at the top of `payment.ts`: a rate (`CARD_PROCESSING_RATE = 0.029`, any name that says card and rate) and a fixed fee with its unit (`CARD_FIXED_FEE_CENTS = 35`, any name that says card, fixed fee, cents); `computeCardFeeCents` reads both. Neither name contains the value. `cents / 100`, `waitMs * 2`, `waitMs = 500`, `"en-US"`, and `"USD"` are untouched. The reply says the rate and the fixed fee are processor contract terms a reader cannot decode from the digits (and the fixed fee has now changed once already), while `100` and `2` are a unit conversion and a doubling that read on the line. Fails if: `0.029` stays bare after the change, or `35` is written inline, or `100`, `2`, or `500` get names, or a name carries the value (`FEE_35_CENTS`, `RATE_2_9_PERCENT`), or the fee constants go to `src/lib`. |
