# Expected decisions — magic literals

| Case | Expected decision |
| --- | --- |
| M01 retry-delay-cap | `fetch-with-retry.ts` gains module-level `const MAX_ATTEMPTS = 5`, `const BASE_DELAY_MS = 250`, and a new `const MAX_DELAY_MS = 3000` (names may vary but each says meaning and unit, e.g. `RETRY_BASE_DELAY_MS`); the loop reads `attempts >= MAX_ATTEMPTS`, computes `BASE_DELAY_MS * attempts`, and the delay becomes `Math.min(MAX_DELAY_MS, ...)` before `onRetry` and `sleep`. `retryAfterSeconds * 1000` stays as written (seconds to ms is on the line) and the `attempts` arithmetic stays. `429` may stay inline (the `status` property names it) or become a named status such as `HTTP_TOO_MANY_REQUESTS`; both pass. The reply says the `5`, `250` and `3000` are limits a reader must decode, so they take names, and the `* 1000` is obvious in its formula. Fails if: `3000` (or `3 * 1000`) is written inline in the `Math.min`; the cap is named after its value (`THREE_SECONDS`, `THREE_THOUSAND`); `5` or `250` are left inline while the new cap is named (inconsistent within one touched function); or `1000` is given a name like `MS_PER_SECOND`. |
| M02 price-thousands-separator | `formatPrice` groups thousands, e.g. via `toLocaleString("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 })` or `new Intl.NumberFormat("en-US", { minimumFractionDigits: 2, maximumFractionDigits: 2 })`, or a regex on the `toFixed(2)` string; sign placement (`-$2.50`) and the other two functions keep their output. A new test like `it("groups thousands with commas", () => expect(formatPrice(123456789)).toBe("$1,234,567.89"))` is added with the fixture value inline. No new constants: `/ 100`, `* 100`, `toFixed(2)`, `minimumFractionDigits: 2`, and the fixture numbers stay inline, and the reply says these are obvious in their formula, option name, or test name. `"en-US"` may stay inline (one call) or take a name if the agent argues two calls read it; both pass. Fails if: `CENTS_PER_DOLLAR`, `DECIMAL_PLACES`, `FRACTION_DIGITS`, `PERCENT_SCALE` or similar are introduced; test fixture numbers are hoisted into named values; or `formatPercent`/`formatSavings` output changes. |
| M03 sidebar-width-change | One `const SIDEBAR_WIDTH_PX = 360` (module level, `CONSTANT_CASE`, name carries unit) placed in the top section or directly above `WorkspaceShell`, read by both design sites: `width: SIDEBAR_WIDTH_PX` and `marginLeft: open ? 0 : -SIDEBAR_WIDTH_PX`. The `maxLength={320}` in `QuickNote` is a different meaning (text length) and stays `320` untouched, or gets its own separately named value (e.g. `QUICK_NOTE_MAX_CHARS`); it is never changed to 360 and never reads `SIDEBAR_WIDTH_PX`. The reply says the width appears twice with one meaning so it takes one name, and the text length is unrelated. Fails if: the two sites are changed to `360` inline with no shared name; `maxLength` is changed to 360 (find-and-replace); `maxLength` reads `SIDEBAR_WIDTH_PX`; the name says the value (`THREE_SIXTY`) or omits the unit while a `px` suffix is used elsewhere in a string (`SIDEBAR_WIDTH` alone is a weak pass, note it); or the value is declared inside the component body as a per-render local. |
| M04 sort-add-trending | `const SORT_OPTIONS = ["newest", "oldest", "popular", "trending"] as const` with `type SortOption = (typeof SORT_OPTIONS)[number]` (or an `as const` array of `{ value, label }` objects with the union derived from `["value"]`), exported from the catalog `lib` (e.g. `sort-products.ts` or a `sort.ts` next to it) because both `sort-products.ts` and `product-sort.tsx` read it. `sortProducts` takes `sort: SortOption` and adds `case "trending": return copy.sort((a, b) => b.trendScore - a.trendScore)`. The `<select>` maps over the array (labels from a `Record<SortOption, string>` map or from the object array) instead of four hand-written `<option>`s. The hand-written `type Sort = ...` union is removed in favour of the derived one. The `"sort"` query-param key and the `"newest"` default may take names (`SORT_PARAM`, `DEFAULT_SORT`) or stay; both pass. Fails if: `enum SortOption` (or any `enum`) is introduced; a fourth `case` and a fourth hand-written `<option>` are added with the option list still duplicated in two files; the union stays hand-written next to an array (two sources of truth); or the array is declared inside the component body. |
| M05 sidebar-collapsed-persist | The root cause is named in the reply: the hook writes `"sidebar:collapsed"` and the shell reads `"sidebar-collapsed"`, so the read never matches. Fix: one `export const SIDEBAR_COLLAPSED_KEY = "sidebar:collapsed"` (or `"sidebar-collapsed"`; either spelling, one value) in `use-sidebar-collapsed.ts`, the module that owns the write; `readStoredCollapsed` imports and reads it. Equally good: the read moves into the hook (`useState(() => ...)` under a `typeof window` guard, or the hook runs the restore effect itself) so one module holds the key and the shell no longer touches storage; the key is still a named module-level value, not an inline string. Fails if: only one string is edited to match the other and two inline literals remain; the key is declared in both files; the key lands in a `src/lib/constants.ts`-style bag file; or the fix adds a fallback that reads both spellings. |
| M06 timers-cancel-on-unmount | `useDebouncedValue`'s effect stores the id and returns `() => clearTimeout(id)`; `SessionWarning`'s effect does the same. `delay = 300` stays an inline default parameter (one reader, and the default is the documentation). `5 * 60 * 1000` stays as the expression (units are on the line) or becomes `const SESSION_WARNING_MS = 5 * 60 * 1000` only if the reply argues a reason (e.g. reuse or a test reads it); both pass. `SearchBox` keeps calling `useDebouncedValue(query)` with no argument. The reply says the values were left because their meaning is on the line. Fails if: `300` is hoisted to a module-level `DEFAULT_DEBOUNCE_MS`/`DEBOUNCE_DELAY` with only the default parameter reading it; the duration is named after its value (`THREE_HUNDRED`, `FIVE_MINUTES` as a bare number `300000`); `5 * 60 * 1000` is rewritten as `300000`; or the cleanup is missing from either file. |
| M07 inbox-limits-naming | `maxItems` becomes `const MAX_ITEMS = 25` (module level, `CONSTANT_CASE`) and a new module-level `const PREVIEW_MAX_CHARS = 140` (name may vary: `PREVIEW_LENGTH`, `PREVIEW_MAX_LENGTH`) sits in the top section or directly above `InboxRow`, its one reader; `InboxRow` renders `message.preview.length > PREVIEW_MAX_CHARS ? \`${message.preview.slice(0, PREVIEW_MAX_CHARS)}…\` : message.preview` (or a small `truncate` helper below `InboxRow`). `STATUS_LABELS` inside `StatusBadge` may stay where it is or move to module level directly above `StatusBadge`; either passes. The reply notes the case rule: module-level unchanging values are `CONSTANT_CASE`, locals are `camelCase`. Fails if: the new limit is `camelCase` at module level (`const previewMaxChars = 140`); `140` is written inline twice (`length > 140` and `slice(0, 140)`); the new limit is declared inside `InboxRow`'s body as a per-render local in `CONSTANT_CASE`; or `maxItems` is edited to `25` and left `camelCase` while the new one is `CONSTANT_CASE` (two styles in one touched file; weak pass only if the reply explains why it was left). |
| M08 premium-tier-check | A helper in the orders feature `lib` (e.g. `src/features/orders/lib/customer-tier.ts`): `export const PREMIUM_TIERS = ["gold", "platinum"] as const satisfies readonly CustomerTier[]` and `export function isPremiumTier(customer: Customer): boolean { return PREMIUM_TIERS.includes(customer.tier) }` (or `isPremiumCustomer`, or taking `tier: CustomerTier`; the `includes` may need a cast or a `Set`). `OrderSummary`, `SupportBanner`, and `ReturnPolicy` all call it; `ReturnPolicy` takes `customer: Customer` and `OrderSummary` passes `order.customer`. The 60/30 day figures may stay in the JSX copy or become named values; both pass. Optional and fine: `CUSTOMER_TIERS` as an `as const` array with `CustomerTier` derived from it. Soft pass (note it): `PREMIUM_TIERS` exported and `.includes(customer.tier)` written inline at all three sites with no function. Fails if: a third copy of `=== "gold" || === "platinum"` is written; the check stays inline in the two existing files and only the new site uses a helper; the helper is a new `src/lib/constants.ts`-style bag or lives under `src/lib` when only the orders feature reads it; `enum CustomerTier` is introduced; or `PREMIUM_TIERS` is declared inside a component body. |
